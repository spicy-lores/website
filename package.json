{
  "name": "spicy-lores-website",
  "version": "1.0.0",
  "author": "Sooraj Parameswaran",
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "export": "next export",
    "deploy": "rm -rf node_modules/.cache && next build && next export && touch out/.nojekyll && git add out/ -f && git commit -m \"Deploy Next.js to gh-pages\" --no-verify && git subtree push --prefix out origin gh-pages"
  },
  "dependencies": {
    "bootstrap": "^5.0.2",
    "next": "latest",
    "react": "^17.0.2",
    "react-bootstrap": "^1.6.1",
    "react-dom": "^17.0.2"
  },
  "license": "MIT",
  "devDependencies": {
    "babel-plugin-transform-define": "^1.3.0"
  }
}
